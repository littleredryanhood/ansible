---
minecraft:
    hosts:
        minecraft.icestation.is:
            ansible_user: ubuntu
            ansible_ssh_private_key_file: ~/.ssh/minecraft_keypair
swarm:
    hosts:
        swarm-queen.icestation.is:
        swarm-worker-00[1:2].icestation.is:
